# 《老街新生》数值设计文档

> **版本**：v0.3（逆向参数锚定版）  
> **配套文档**：`产品需求文档PRD.md` v0.3  
> **覆盖范围**：PRD 第 5 章 P0 核心系统（店铺经营 / 繁荣值 / 每日结算 / 货币经济 / 离线收益）  
> **文档性质**：**数值框架**——定义经济模型的数学结构、公式体系与配置表 schema。**所有具体数值为占位/示例，须经建模与灰度校准后锁定**，示例值标注 `‹示例›`。

**修订记录**：
- v0.1（2026-08-20）：初版框架，基于 PRD v0.1
- v0.2（2026-08-20）：同步 PRD v0.3，补充游戏日最大时长 `T_day_max`、欠租处理 `rent_owe_mode`、G_max 截断落地规则、离线时长负数归零、重复策略卡处理 `duplicate_handle`；生成全套 8 张配置表 Excel
- v0.3（2026-09-18）：新增 §3.4 逆向实测参数快照（录屏逆向配置表锚定）；待确认清单 #1/2/3/4/9/10 标已逆向、#5/6/7 标逆向推断待实机；`economy_sim.py` PARAMS 已同步

---

## 0. 文档说明

### 0.1 设计目标
1. 用可计算的数学模型描述整个经济系统，消除歧义。
2. 所有可调参数外置为配置表，实现"改表即改数值、无需改代码"。
3. 提供平衡性校验的量化标准，支撑数据驱动调优。

### 0.2 符号约定

| 符号 | 含义 |
|------|------|
| `L` | 店铺等级（level） |
| `T(L)` | 等级 L 时的每小时客流量（traffic） |
| `S(L)` | 等级 L 时的客单价（base spend） |
| `C(L)` | 从 L 升到 L+1 的金币成本（cost） |
| `P` | 当前繁荣值（prosperity） |
| `G` | 全局增益系数（global multiplier，来自策略卡/宣传/装饰） |
| `R_i` | 单店铺 i 的金币产出速率（币/秒） |
| `R_total` | 全店铺总产出速率 |
| `η_off` | 离线效率系数 |
| `t` | 时间（秒） |

### 0.3 核心设计常量（全局，可配）

| 常量 | 符号 | 示例值 | 说明 |
|------|------|--------|------|
| 时间单位换算 | — | 1 小时 = 3600 秒 | 客流量以小时计，产出以秒结算 |
| 离线效率 | `η_off` | `‹0.5›` | 离线产出为在线的比例 |
| 离线时长上限 | `t_off_max` | `‹8h›` | 离线收益封顶时长 |
| 广告翻倍倍率 | `k_ad` | `‹2.0›` | 激励视频收益倍数 |
| 数值显示进制 | — | K/M/B/T/aa… | 大数缩写规则 |

---

## 1. 经济模型总览

### 1.1 核心产出链

```
店铺等级 L ──T(L),S(L)──► 单店秒产出 R_i ──Σ──► 总产出 R_total ──×G──► 金币入账
                                                              │
金币 ──► 升级成本 C(L) ──► 等级↑ ──► 产出↑（正循环）           │
   ▲                                                          │
   └────────────── 离线补算 / 广告翻倍 ◄──────────────────────┘
```

### 1.2 核心平衡原则
- **产出增速 > 成本增速** 会导致收益爆炸；**产出增速 < 成本增速** 会导致卡关。健康放置曲线要求：升级带来的产出增益，其"回本时间"随等级平缓上升，形成"越到后期升级越慎重"的决策感。
- 定义**回本时间** `PB(L) = C(L) / ΔR(L)`，其中 `ΔR(L)` 为该次升级带来的秒产出增量。`PB(L)` 应随 L 缓慢单调上升，控制在可接受等待区间。

---

## 2. 店铺经营数值模型（P0）

### 2.1 客流量成长公式
每小时客流量随等级线性或缓增长：

```
T(L) = T_base × (1 + a_t × (L - 1))
```

- `T_base`：1 级基础客流量（每店铺类型不同，配置表定义）
- `a_t`：客流量每级增长率 `‹示例 0.12›`
- 说明：客流量增长设计为**平缓线性**，避免与客单价叠乘造成双指数爆炸。

### 2.2 客单价成长公式
客单价随等级指数增长（收益成长主要来源）：

```
S(L) = S_base × (r_s)^(L - 1)
```

- `S_base`：1 级基础客单价
- `r_s`：客单价每级增长率 `‹示例 1.08›`

### 2.3 单店产出速率

```
R_i = T(L) × S(L) / 3600      （币/秒）
```

即：每小时客流量 × 客单价 ÷ 3600，换算为每秒产出。

### 2.4 总产出速率（含全局增益）

```
R_total = ( Σ R_i ) × G
```

`G` 为全局增益系数，定义见 §5。

### 2.5 升级成本公式
成本随等级指数增长，且增速略高于客单价增速，保证回本时间可控：

```
C(L) = C_base × (r_c)^(L - 1)
```

- `C_base`：首次升级基础成本
- `r_c`：成本增长率 `‹示例 1.10›`，**须满足 `r_c > r_s`**（成本增速 > 客单价增速，否则收益爆炸）

### 2.6 回本时间校验
```
ΔR(L) = R_i(L+1) - R_i(L)
PB(L) = C(L) / ( ΔR(L) × G )      （秒）
```
**平衡约束**：`PB(L)` 应随 L 单调缓升，全程控制在 `‹目标区间，如 30 秒 ~ 30 分钟›`。若出现骤升/骤降即为数值断点，需调 `r_c` / `r_s`。

### 2.7 店铺解锁
店铺 i 的解锁条件：
```
P ≥ P_unlock(i)   且   金币 ≥ BuildCost(i)
```

---

## 3. 繁荣值模型（P0）

### 3.1 繁荣值产出
繁荣值来自店铺成长贡献（只增不减）：

```
ΔP = Σ over升级/修建事件 [ ProsperityGain(shopType, L) ]
```

- 每次店铺升级/修建按配置贡献繁荣值，等级越高贡献越多（配置表 `prosperity_gain` 定义）。

### 3.2 解锁阈值曲线
新店铺/区域的繁荣阈值递增：

```
P_unlock(n) = P_0 × (r_p)^(n - 1)      或采用手工配置的阶梯表
```

- `n`：第 n 个解锁项
- `r_p`：阈值递增率 `‹示例 1.5›`
- **建议采用手工配置阶梯**（配置表逐项定义），比纯公式更利于关卡节奏把控。

### 3.3 里程碑
繁荣值达特定值触发里程碑奖励（配置表 `prosperity_milestone` 定义阈值与奖励）。

### 3.4 逆向实测参数快照（2026-09-18）
> 以下数值来自竞品录屏逆向配置表（`shop_config.xlsx` 等 8 张），非本游戏设计值；
> 本游戏 M1 建模以这些值为锚点校准，标 `【待实机】` 者仍需真机复核。

**店铺参数（shop_config.xlsx 逆向）**

| 店 | T_base | S_base | a_t | r_s | C_base | r_c | 解锁繁荣 | 修建成本 | 修建时长 |
|---|---|---|---|---|---|---|---|---|---|
| 配钥匙铺 | 100 | 10 | 0.12 | 1.08 | 50 | 1.10 | 0 | 0 | — |
| 自行车铺 | 120 | 12 | 0.12 | 1.08 | 60 | 1.10 | 260 | 5000 | 60s |
| 纪念品铺 | 140 | 15 | 0.12 | 1.08 | 80 | 1.10 | 500 | 12000 | 90s |
| 店4/店5 | 160 / 180 | 18 / 22 | 0.12 / 0.12 | 1.08 / 1.09 | 100 / 130 | 1.10 / 1.11 | 900 / 1500 | 20000 / 30000 | 示例 |

- `max_level = 0` 表示等级无上限。
- **店4/店5 为设计外推值**（无逆向数据）：按店1→3 规律外推——`T_base` 等差 +20、`S_base` 10/12/15→18/22、`C_base` 50/60/80→100/130、`build_cost` 5000/12000→20000/30000；解锁阈值 900/1500 为 `prosperity_unlock.xlsx` 逆向。实机/逆向确认前仅作仿真占位。

**繁荣贡献区间表（prosperity_gain.xlsx 逆向 + 店4/5 外推）**

| 店 | 等级区间 | 每级繁荣 | 来源 |
|---|---|---|---|
| 配钥匙铺 | 1–10 级 / 11–30 级 | 5 / 8 | 逆向 |
| 自行车铺 | 1–10 级 | 6 | 逆向 |
| 纪念品铺 | 1–20 级 | 10 | 逆向 |
| 特色店4 | 1–10 级 / 11–30 级 | 9 / 12 | 外推设计 |
| 特色店5 | 1–20 级 | 12 | 外推设计 |

**解锁阶梯（prosperity_unlock.xlsx 逆向）**：第 2/3/4/5 店需繁荣 **260 / 500 / 900 / 1500**。

**里程碑（prosperity_milestone.xlsx 逆向）**

| 繁荣阈值 | 奖励 |
|---|---|
| 110 | 5000 币 |
| 500 | 3600 加速 |
| 1000 | 广告翻倍 |
| 2000 | 抽卡 |

**每日结算（daily_config.xlsx 逆向）**：顾客目标 60/80/100/125/150；租金 200/230/265/305/351（≈ 200×1.15ⁿ⁻¹，误差 <2%，仿真沿用公式近似）。

**策略卡（card_config.xlsx 逆向 + P0-Lite 扩展）**：保底 5 次、重复→碎片/升级/金币；效果有加区（additive）与乘区（multiplicative）两类。P0-Lite 卡池设计为 8 张（逆向 4 张 + 新增 4 张）：

| ID | 稀有度 | 类型 | 目标 | 数值 | 重复处理 | 来源 |
|---|---|---|---|---|---|---|
| 1001 | common | additive | traffic_all | +15% | fragment | 逆向 |
| 1002 | common | additive | spend_all | +20% | fragment | 逆向 |
| 1003 | rare | multiplicative | traffic_all | +25% | upgrade | 逆向 |
| 1004 | rare | additive | rent_reduce | -10% | coin | 逆向 |
| 1005 | common | additive | traffic_all | +10% | fragment | 新增·前期弱卡 |
| 1006 | common | additive | spend_all | +10% | fragment | 新增·前期弱卡 |
| 1007 | common | additive | rent_reduce | -5% | coin | 新增·弱减免 |
| 1008 | rare | multiplicative | spend_all | +20% | upgrade | 新增·乘区客单价 |

- 设计意图：1005/1006 为新手前期易得的弱卡（+10%），1001/1002 为中期主力（+15/20%），1003/1008 为后期乘区爆点（+25/20%），1004/1007 为经营减压卡。稀有度 common:rare = 6:2。
- 仿真暂用逆向 4 张（1001–1004）作示例玩家持有集；1005–1008 待 P1 抽卡节奏实测后接入。

**仍待实机（global_constants.xlsx 推断值）**：`η_off=0.5`、离线封顶 8h、广告翻倍 k=2、`T_day_max=8h`、欠租 `deduct_first`——见 §4.2 / §6。

**策略卡接入仿真（2026-09-18）**：`economy_sim.py` 已按 §5.1 实现动态 G——`G=(1+promo_buff+Σadditive卡)×(1+Σmultiplicative卡)`，`min(g_max=10)`；租金减免卡（rent_reduce）单独乘到当日租金。示例卡激活节奏：D3 获 1001(+15%traffic)、D5 获 1002(+20%spend)、D8 获 1003(+25%traffic乘区)、D10 获 1004(租金-10%)；仿真 G 从 1.50→1.65→1.85→2.31 阶梯上升。激活日为示例假设，待实机校准抽卡节奏。

**名称法务补充检索（2026-09-18）**：公开网络复查，"老街新生"全部为文旅街区改造宣传语/新闻报道（武汉黎黄陂路、自贡自流井、青岛上街里等），**游戏领域无同名产品、无同名商标**；但该词系文旅高频宣传语，商标显著性弱。正式中国商标网检索（第 9/41/42 类，sbj.cnipa.gov.cn）需人工完成，仍为 M1 前待办；建议作代号、上架前确定备选名。

---

## 4. 每日结算模型（P0）

### 4.1 游戏日结束条件（PRD v0.3 §5.3 修订）
```
主条件：完成顾客数 CustDone ≥ CustTarget(day)      → 可主动结算推进
强制条件：游戏日现实时长 ≥ T_day_max                → 强制结算推进
```
- `T_day_max`：游戏日最大现实时长 `‹示例 8 小时 = 28800 秒›` `【待验证】`。**防套利关键参数**——玩家不可无限卡在同一天刷金币，达阈值即触发强制结算并扣租金。
- 无论主动结算还是强制结算，均执行租金扣减（见 §4.4）。

### 4.2 顾客目标曲线
每日目标随天数递增：
```
CustTarget(day) = Cust_0 × (1 + a_d × (day - 1))       （线性）
   或  Cust_0 × (r_d)^(day-1)                           （指数，视节奏选择）
```

### 4.3 租金曲线
每日租金随天数递增，构成经营压力：
```
Rent(day) = Rent_base × (r_rent)^(day - 1)
```
- **平衡约束**：`Rent(day)` 应占当日玩家预期总产出的一个**受控比例**（如 `‹10%~30%›`），既有压力又不至卡死。定义租金压力比 `RentRatio(day) = Rent(day) / DailyIncome(day)`，须落在目标带内。

### 4.4 当日收益结算（含欠租处理，PRD v0.3 §5.3 / 附录 G）
```
DailyIncome = 当日累计金币产出
结算时租金扣减：
  if 当前金币 ≥ Rent(day):
      当前金币 -= Rent(day)
  else:
      按 rent_owe_mode 处理（见下）
```

**欠租处理模式**（`rent_owe_mode`，配置表 `global_constants` 定义）：
- **`deduct_first`（推荐）**：当前金币不足时，先扣完所有金币清零，剩余欠租记为 `owe`；后续收益优先抵扣 `owe` 直至还清，期间玩家金币显示为 0。**不破产、不负数**（治愈系定位）。
- **`accumulate`**：欠租累积为负债，金币可为负，后续收益逐步回正。此模式压力更大，需数值建模确认玩家可承受。
- 方案须在 M1 数值建模时锁定，全局唯一，配置表 `global_constants.rent_owe_mode` 字段定义。

### 4.5 未达标处理（治愈系定位）
```

### 4.5 未达标处理（治愈系定位）
- 未达顾客目标：**不失败、不惩罚**，仅不推进天数，继续经营直至达标。租金在推进日时才扣除。

---

## 5. 全局增益系数 G（P0 基础 + P1 策略卡）

### 5.1 增益结算模型（加区先加、乘区后乘）
```
G = ( 1 + Σ additive_i ) × Π ( 1 + multiplicative_j )
```

- **加成区** `additive_i`：来自基础 buff、宣传、部分卡牌（线性叠加）。
- **乘算区** `multiplicative_j`：来自高稀有度策略卡（乘算叠加）。
- **P0 阶段**：G 仅由宣传 buff 与基础值构成（策略卡为 P1）。P0 可先实现 `G = 1 + PromoBuff`。

### 5.2 数值风控约束（PRD v0.3 §5.4.4 / 附录 G）
- **G_max 上限截断**：定义全局增益上限 `G_max` `‹示例 10.0›` `【待验证】`（配置表 `global_constants.g_max`）。计算出的 `G` 若超出，截断为 `G_max`，超出部分不生效。**落地规则**：
  ```
  G_raw = ( 1 + Σ additive_i ) × Π ( 1 + multiplicative_j )
  G = min(G_raw, G_max)
  ```
  防止策略卡叠乘导致收益指数爆炸破坏经济。
- 乘算区单项数值与总项数均须在卡牌设计时控制，配合 `G_max` 双重风控。
- 结算顺序固定为"先加后乘"，全项目统一，禁止歧义实现。

---

## 6. 离线收益模型（P0）

### 6.1 离线收益公式（含防时间回拨，PRD v0.3 §15.2 / 附录 G）
```
Δt_offline_raw = 服务端当前时间戳 - 上次离线时间戳
Δt_offline = max(0, Δt_offline_raw)    // 负数归零，防时间回拨作弊
Δt_capped = min(Δt_offline, t_off_max)
OfflineGain = R_total × Δt_capped × η_off
```

- `Δt_offline_raw`：服务端时间戳差值（**禁止信任本地时间**，见 PRD TR-9.2 / §10.4）
- **时间回拨检测**：若 `Δt_offline_raw < 0`（本地改时间前调），归零处理不发收益，并上报作弊埋点 `offline_time_rollback`。PRD v0.3 附录 G.1 新增。
- `t_off_max`：离线封顶时长 `‹8h = 28800 秒›` `【待验证】`
- `η_off`：离线效率系数 `‹0.5›`

### 6.2 广告翻倍
```
OfflineGain_final = OfflineGain × (1 或 k_ad)      // 看广告则 ×k_ad
```

---

## 7. 配置表结构（Schema）

> 所有表建议 JSON / CSV 外置，支持热更。以下为字段定义，`‹›` 为示例值。

### 7.1 店铺基础表 `shop_config`

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| shop_id | int | 店铺唯一 ID | 1 |
| shop_type | string | 类型标识 | `locksmith` |
| name_key | string | 名称多语言 key（原创文案） | `shop_name_1` |
| t_base | float | 1级基础客流量 | `‹100›` |
| a_t | float | 客流量每级增长率 | `‹0.12›` |
| s_base | float | 1级基础客单价 | `‹10›` |
| r_s | float | 客单价每级增长率 | `‹1.08›` |
| c_base | float | 首次升级基础成本 | `‹50›` |
| r_c | float | 成本增长率(>r_s) | `‹1.10›` |
| build_cost | float | 修建成本 | `‹5000›` |
| unlock_prosperity | int | 解锁所需繁荣值 | `‹260›` |
| max_level | int | 等级上限(0=无上限) | `‹0›` |
| build_time | int | 建造时长(秒) | `‹60›` |

### 7.2 繁荣值贡献表 `prosperity_gain`

| 字段 | 类型 | 说明 |
|------|------|------|
| shop_type | string | 店铺类型 |
| level_from | int | 等级区间下界 |
| level_to | int | 等级区间上界 |
| gain_per_level | float | 该区间每级繁荣贡献 |

### 7.3 繁荣解锁阶梯表 `prosperity_unlock`

| 字段 | 类型 | 说明 |
|------|------|------|
| unlock_order | int | 第 n 个解锁项 |
| target_shop_id | int | 解锁的店铺/区域 ID |
| required_prosperity | int | 所需繁荣值 |

### 7.4 繁荣里程碑表 `prosperity_milestone`

| 字段 | 类型 | 说明 |
|------|------|------|
| milestone_id | int | 里程碑 ID |
| prosperity_threshold | int | 触发阈值 |
| reward_type | enum | coin/speedup/ad_double/card_draw |
| reward_value | float | 奖励数值 |

### 7.5 每日结算表 `daily_config`

| 字段 | 类型 | 说明 |
|------|------|------|
| day | int | 游戏日(或用公式参数化) |
| cust_target | int | 当日顾客目标 |
| rent | float | 当日租金 |
| challenge_id | int | 关联挑战(可空) |

> 若采用公式生成，则本表退化为参数表：`cust_0, a_d, rent_base, r_rent` 等。

### 7.6 全局常量表 `global_constants`（PRD v0.3 同步）

| 字段 | 类型 | 说明 | 示例 |
|------|------|------|------|
| key | string | 参数名 | — |
| value | float | 参数值 | — |
| unit | string | 单位 | — |
| description | string | 说明 | — |

**已定义常量**（Excel 表 `global_constants.xlsx` 同步）：
- `eta_offline`：离线效率 `‹0.5›`
- `t_off_max`：离线封顶秒数 `‹28800›` (8h)
- `k_ad`：广告翻倍倍率 `‹2.0›`
- `g_max`：全局增益上限 `‹10.0›` **（PRD v0.3 §5.2）**
- `promo_buff`：宣传增益(P0) `‹0.5›`
- `promo_duration`：宣传持续秒数 `‹300›`
- **`t_day_max`**：游戏日最大时长秒数 `‹28800›` (8h) **（PRD v0.3 §4.1 新增）**
- **`rent_owe_mode`**：欠租处理模式 `‹deduct_first›` (enum: deduct_first/accumulate) **（PRD v0.3 §4.4 新增）**

> 所有常量为 `【待验证】` 设计假设，须 M1 建模锁定。

### 7.7 策略卡表 `card_config`（P1 完整版 + P0-Lite，PRD v0.3 §5.4）

| 字段 | 类型 | 说明 |
|------|------|------|
| card_id | int | 卡牌 ID |
| rarity | enum | common/rare/epic/legendary |
| effect_type | enum | additive/multiplicative |
| target | enum | traffic_all/spend_all/shop_type_X/rent_reduce |
| value | float | 增益数值 |
| pity_count | int | 保底刷新次数(如 5) |
| **duplicate_handle** | enum | **重复卡处理：fragment(碎片)/upgrade(直接升级)/coin(折金币)** **（PRD v0.3 §5.4.5 新增）** |

**P0-Lite 策略卡子集**（PRD §5.4.0，MVP 必做）：
- 仅 `【待验证】` 少量（6-10 张）基础卡，`rarity` 全为 `common`，`effect_type` 仅 `additive`
- 无图鉴、无套装、无稀有度分层
- 目的：让 MVP 具备差异化玩法，供内测收集反馈
- 完整卡池、保底、图鉴、`duplicate_handle` 机制在 P1 实现

### 7.8 每日任务表 `daily_task`（P1，预留）

| 字段 | 类型 | 说明 |
|------|------|------|
| task_id | int | 任务 ID |
| task_type | enum | **boost_collect**/serve_customer/online_time（PRD v0.3 §6.1 改为间接行为） |
| tier1_target / tier2_target / tier3_target | int | 阶梯目标（可空） |
| reward_per_tier | string | 奖励格式：类型_数值，如 `2min_income / coin_500 / speedup_600` |

> 注：原 `watch_ad` 任务类型改为 `boost_collect`（完成收益翻倍领取次数），规避微信广告诱导判定风险（PRD v0.3 §6.1 / 评审三.5）。

---

## 8. 平衡性校验方法（PRD v0.3 同步）

### 8.1 关键校验指标

| 指标 | 定义 | 健康区间 `【待验证】` |
|------|------|----------|
| 回本时间 PB(L) | `C(L)/(ΔR(L)×G)` | 单调缓升，`‹30s~30min›` |
| 租金压力比 RentRatio(day) | `Rent(day)/DailyIncome(day)` | `‹10%~30%›` |
| 首个新店解锁时长 | FTUE 内解锁第2店的挂机时长 | `‹≤3 分钟›` |
| 单日理论产出增长率 | `DailyIncome(day+1)/DailyIncome(day)` | 平滑无断点 |
| 广告收益感 | 广告翻倍带来的进度加速比 | 明显但非必需 |

### 8.2 建模方法
1. 用表格模型（Excel/脚本）模拟 1~N 天玩家进度曲线。
2. 绘制 `PB(L)`、`RentRatio(day)`、累计金币/繁荣曲线，检查单调性与断点。
3. 分"重度看广告 / 不看广告"两条路径分别模拟，确保两者均可正常推进（守留存 + 促变现）。
4. 灰度期埋点回收真实曲线，校准 `r_s / r_c / r_p / r_rent` 等关键增长率。

### 8.3 数值调参优先级
`r_c` 与 `r_s` 的差值 → 决定整体收益松紧（最高优先）；`r_rent` → 决定经营压力；`r_p` / 解锁阶梯 → 决定内容释放节奏；`η_off` / `k_ad` → 决定回访与广告动机。

---

## 9. 待建模确认清单（对齐 PRD v0.3 §14）

> 阻塞级别：🔴 立项/M1 阻塞（须先确认）；🟡 MVP 可用假设先行、P1 迭代确认。

| # | 待确认事项 | 阻塞级别 | 当前假设 |
|---|-----------|---------|---------|
| 1 | 各店铺类型的 `t_base / s_base / c_base` 初始档位与差异化定位？ | 🔴 M1 阻塞 | ✅ 已逆向：配钥匙铺(100/10/50)、自行车铺(120/12/60)、纪念品铺(140/15/80)，见 §3.4 |
| 2 | `r_s` 与 `r_c` 的最终差值（决定收益曲线松紧）？ | 🔴 M1 阻塞 | ✅ 已逆向：`r_s=1.08, r_c=1.10, a_t=0.12`（shop_config.xlsx） |
| 3 | 繁荣解锁采用公式还是手工阶梯？各阈值？ | 🔴 立项阻塞 | ✅ 已逆向：手工阶梯 260/500/900/1500（prosperity_unlock.xlsx） |
| 4 | 顾客目标与租金曲线形态（线性/指数）及参数？ | 🔴 M1 阻塞 | ✅ 已逆向：顾客 60→150、租金 200→351（daily_config.xlsx） |
| 5 | **游戏日最大时长 `T_day_max` 真实值？**（PRD v0.3 新增） | 🔴 立项阻塞 | ⚠️ 逆向推断 8h (28800s)，【待实机】 |
| 6 | **欠租处理模式 `rent_owe_mode`？**（PRD v0.3 新增） | 🔴 M1 阻塞 | ⚠️ 逆向推断 `deduct_first`，【待实机】 |
| 7 | 离线效率 `η_off`、封顶 `t_off_max`、广告倍率 `k_ad` 的最终值？ | 🔴 M1 阻塞 | ⚠️ 逆向推断 `η=0.5 / 8h / k=2.0`，【待实机】（影响次留） |
| 8 | **全局增益上限 `G_max` 落地值？**（PRD v0.3 §5.2） | 🔴 M1 阻塞 | 假设 10.0（global_constants.xlsx g_max=10） |
| 9 | **重复策略卡处理方式 `duplicate_handle`？**（PRD v0.3 §5.4.5） | 🟡 P1 确认 | ✅ 已逆向：fragment/upgrade/coin 三种（card_config.xlsx） |
| 10 | P0-Lite 策略卡数量与数值范围？ | 🟡 MVP 确认 | ✅ 已逆向 4 张（2c+2r）+保底 5；P0-Lite 扩 8 张（原型） |
| 11 | 大数显示进制与精度方案（防 JS Number 溢出）？ | 🟡 M2 确认 | K/M/B/T + 大数库 |

> **声明**：本文档为数值框架，定义结构与公式，不含最终平衡数值。所有 `‹示例›` 与 `【待验证】` 项须经建模仿真 + 灰度测试后锁定，并同步回写本文档正式版。配置表 Excel 已生成（8 张），字段 schema 与本文档 §7 一致。

